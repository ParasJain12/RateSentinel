apiVersion: v1
kind: Service
metadata:
  name: ratesentinel-service
  namespace: ratesentinel
spec:
  selector:
    app: ratesentinel
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080